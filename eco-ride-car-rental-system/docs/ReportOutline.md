# Report Outline (mapped to marking scheme)

This repository already includes the material you need for the single document:

1. **Class Diagram**  
   - Export `docs/class-diagram.puml` to PNG (replace `K1234567` with your real ID).

2. **Software Implementation**  
   - `src/main/java` packages: `domain`, `repository`, `service`, `cli`  
   - OOP concepts: encapsulation (getters/setters), inheritance (Vehicle + subclasses, Customer + subclasses), polymorphism (overridden `display*Details()` methods), abstraction (interfaces for repositories, policy class).

3. **Test Cases**  
   - See `src/test/java` (JUnit) and `docs/TestCases.md` (manual table).

4. **Conclusion**  
   - Summarize conformance, challenges, improvements.

5. **Project link**  
   - Your GitHub/Drive URL here.
